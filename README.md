# MS_LJMR — Multivariate Data Analysis, revised code base + book

Revised and extended course code for **BIOL 943 (Multivariate Methods)**, integrating
the machine-learning course material per the **revised Fall-2026 schedule**.
Original teaching scripts by Jorge Soberón, Laura Jiménez and Marlon E. Cobos;
revision, ML integration, verification, and the accompanying book by
**Luis J. Madrigal-Roca**.

## Layout

```
MS_LJMR/
├── R/            revised, portable R scripts (no setwd(), no x11())
│   ├── 00_utils.R          shared helpers + get_data() (real-file-or-fallback)
│   ├── 05_Clustering.R     k-means + hierarchical
│   ├── 09_PCA.R            PCA I–III with PCoA folded in
│   ├── 21_NN_DL.R          neural nets (nnet track + keras template)  [NEW]
│   ├── 25_SupervisedML.R   trees / RF / boosting + caret              [NEW]
│   ├── 26_UnsupervisedML.R PCA vs t-SNE vs UMAP                       [NEW]
│   └── 27_LLM_Embeddings.R embeddings = multivariate vectors          [NEW]
├── data/         drop the real course CSV/RDS here (auto-detected)
├── figs/         figures used by the book (regenerated by verify/)
├── verify/       Python reproductions that CHECK the R + render figures
│   ├── make_figures.py     core chapters + verify_log.txt
│   └── extra_figures.py    dissimilarity / correlation / MDS figures
└── book/         MS_LJMR_Multivariate.tex  (single-file Wiley-style book)
```

## Authorship

- Scripts derived from the original BIOL 943 course code carry their **original authors**
  (Jorge Soberón, Laura Jiménez, Marlon E. Cobos, as applicable) in the header, with a
  "Revised for the MS_LJMR edition by Luis J. Madrigal-Roca" line.
- Scripts derived from Luis's ML course (`21_NN_DL`, `25_SupervisedML`) and the new
  sessions (`26_UnsupervisedML`, `27_LLM_Embeddings`) and the shared `00_utils.R` are
  authored by **Luis J. Madrigal-Roca**.

## How the data loader works — and what data is actually used

Every script gets its data through `get_data("<name>")` in `R/00_utils.R`, which tries,
in order:

1. the **real course file** if present in `MS_LJMR/data/` (e.g. `ButterfliesQRoo2.csv`,
   `BiodivCountries.csv`, `leukemiaExpressionSubset.rds`, …);
2. the **genuine in-package dataset** when the relevant package is installed — currently
   `ade4::doubs` (the real river-fish data used in the CA / canonical / RDA sessions);
3. a **frozen demo file** in `data/demo/` (shared byte-for-byte with the Python verifier
   so deterministic results reproduce exactly);
4. a **seeded, structure-faithful simulation** with the same columns.

So the scripts run today, unmodified, and silently upgrade to the classroom data the
moment you add the real files — no path editing, ever.

### Data provenance (important)

The professor's distributed data files were **not** available in this build environment,
so they are **not** yet used. Concretely:

- **Genuine data, identical to class:** `iris`, `mtcars` (R built-ins); `doubs` loads the
  real `ade4::doubs` on any machine with `ade4` installed.
- **Seeded stand-ins (until you add the real files):** `ButterfliesQRoo2`, `BiodivCountries`,
  `NeotomaMorphoEnvir`, `Limenitis_archippus`, `leukemiaExpressionSubset`, `CitiesEurope`,
  `speciesCrawley3`, `taxon`, `CountriesToLive`, plus the ENM/PAM demos. Drop the real
  CSV/RDS files into `MS_LJMR/data/` (using the original filenames) and the loader — and a
  re-run of `verify/freeze_and_reference.py` — will switch to them automatically.

## Reproducing

```bash
# figures + numerical verification (no R needed)
cd verify && python3 make_figures.py && python3 extra_figures.py

# the book
cd ../book && pdflatex MS_LJMR_Multivariate.tex   # run 2–3× for the TOC
```

## Verification note

No R interpreter was available in the build environment, so every method's key
quantities were recomputed independently in Python (NumPy / SciPy / scikit-learn /
statsmodels) and logged to `verify/verify_log.txt`; the R scripts were written to match.
Reference values: iris PCA variance 73.0 / 22.9 / 3.7 / 0.5 %; k-means recovers the
species at 89 % agreement; LDA hold-out accuracy 1.00; tree/forest/boosting ≈ 0.96
(5-fold); `king − man + woman → queen` resolves correctly.

## What changed vs. the originals

- Removed hard-coded `setwd()` and Windows-only `x11()`; portable `open_dev()`/`with_fig()`.
- Folded PCoA into the PCA arc; retired standalone visualization/ggplot2 sessions.
- Added four ML chapters/scripts adapted from `UnitCART`, `UnitCARET`, `UnitNN`.
- Fixed original bugs: undefined `d_euc2` (dissimilarities), `Qroo3`/`QRoo3` clash (MDS biplot).
# Multivariate_Class_Book
